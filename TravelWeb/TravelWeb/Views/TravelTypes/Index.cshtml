@model IEnumerable<TravelWeb.Models.TravelType>

@{
    ViewBag.Title = "Index";
}

<h2>Аялал төрөл</h2>

<p>
    @Html.ActionLink(" Бүртгэх", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Нэр
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TravelTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TravelTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TravelTypeID })
        </td>
    </tr>
}

</table>
